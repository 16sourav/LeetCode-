class Solution {
public:
    bool isPalindrome(int x) {
        long p=0,z=x;
        if(x<0) return 0;
        while(x!=0)
        {
            p=p*10+x%10;
            x/=10;
        }
        if(p==z) return 1;
        return 0;
    }
};
