class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector<int> vect;
        int x=0;
        for(int i=0;i<nums.size();i++)
        {
            x=nums[nums[i]];
            vect.push_back(x);
        }
        return vect;
        
    }
};
